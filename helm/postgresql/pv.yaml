apiVersion: v1
kind: PersistentVolume
metadata:
  namespace: postgresql
  name: postgres-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/for/example"